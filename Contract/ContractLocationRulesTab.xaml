<RazerBase:ScreenBase x:Class="Contract.ContractLocationRulesTab" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:RazerBase="clr-namespace:RazerBase;assembly=RazerBase"
             xmlns:converters="clr-namespace:RazerConverters;assembly=RazerConverters"
             xmlns:igDP="http://infragistics.com/DataPresenter" 
             xmlns:igEditors="http://infragistics.com/Editors"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1080"
             Height="Auto" Width="Auto" Background="{DynamicResource ApplicationBackground}">
    <!--Resource area below added to allow for row color coding based on if the attachment is ediatble or not-->
    <!--The resource is needed to support the color coding of the rows in the grid-->
    <UserControl.Resources>

        <Style TargetType="{x:Type igDP:DataRecordPresenter}">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="{Binding Path=Cells[color_status].Value, Converter={StaticResource AttachmentHighlightConverter}}" />
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type igEditors:XamCheckEditor}" x:Key="customCheckEditor">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type igEditors:XamCheckEditor}">
                        <CheckBox HorizontalAlignment="Center" IsChecked="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, Mode=TwoWay}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="mainGrid" Width="Auto">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        <GroupBox Name="grpBatch" Content="Batch Processing" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="6">

        </GroupBox>
        <RazerBase:ucLabelCheckBox Name="chkBatch" Grid.Row="1" Grid.Column="1" LabelText="" LabelWidth="40" Height="110"  Checked="chkBatch_Checked" UnChecked="chkBatch_UnChecked" />
        <Border Name="brdBatch" Grid.ColumnSpan="4" Grid.Row="2" BorderBrush="Gray" BorderThickness="0,0,0,1" IsEnabled="False" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MinWidth="50"/>
                    <ColumnDefinition MinWidth="50"/>
                    <ColumnDefinition MinWidth="50"/>
                    <ColumnDefinition MinWidth="50"/>
                    <ColumnDefinition MinWidth="50"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition MinHeight="40"/>
                    <RowDefinition MinHeight="40"/>
                </Grid.RowDefinitions>
                <RazerBase:ucLabelCheckBox Name="chkOverride" LabelText="Override" Grid.Row="0" Grid.Column="0" IsEnabled="{Binding ElementName=chkBatch, Path=IsChecked}" />
                <RazerBase:ucLabelDatePicker Name="dtpEffStartDate" LabelText="Eff Start Date:" Grid.Row="0" Grid.Column="1" IsEnabled="{Binding ElementName=chkBatch, Path=IsChecked}" />
                <RazerBase:ucLabelDatePicker Name="dtpEffEndDate" LabelText="Eff End Date:" Grid.Row="0" Grid.Column="2" IsEnabled="{Binding ElementName=chkBatch, Path=IsChecked}" />
                <RazerBase:ucLabelCheckBox Name="chkRoll" LabelText="Roll Forward" Grid.Row="0" Grid.Column="3" IsEnabled="{Binding ElementName=chkBatch, Path=IsChecked}" />
                <Button Name="btnApply" Grid.Row="0" Grid.Column="4" MaxWidth="85" IsEnabled="{Binding ElementName=chkBatch, Path=IsChecked}" Click="btnApply_Click">Apply</Button>
            </Grid>
        </Border>
        
        <!--<RazerBase:ucColorKey Name="OpsColorKey" RedLabel="In Process" RedVisible="True" GreenVisible="False" YellowVisible="False" TealVisible="False" Grid.Row="1" Grid.Column="1" />-->
        
        <RazerBase:ucBaseGrid Name="gLocationRules" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" ></RazerBase:ucBaseGrid>
        <!--<Button Name="btnPrebill" Height="30" Width="150" Grid.Column="2" Grid.Row="1" Click="RunPrebill">Run Prebill</Button>-->

    </Grid>
</RazerBase:ScreenBase>
