<local:ScreenBase x:Class="Invoice.InvoiceDetailTab" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RazerBase;assembly=RazerBase"
             xmlns:igDP="http://infragistics.com/DataPresenter" 
             mc:Ignorable="d" 
             d:DesignHeight="9500" d:DesignWidth="716"
             Height="Auto" Width="Auto" Background="{DynamicResource ApplicationBackground}">
    <Border BorderBrush="Gray" BorderThickness="1,0,0,0">
        <Grid>
        <Grid.ColumnDefinitions>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
        <local:ucBaseGrid Name="gInvoiceDetail" Grid.Column="1" Grid.ColumnSpan="2" ></local:ucBaseGrid>
      
    </Grid>
    </Border>
    <!--</ScrollViewer>-->
</local:ScreenBase>
