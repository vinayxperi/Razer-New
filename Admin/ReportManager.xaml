    <RazerBase:ScreenBase x:Class="Admin.ReportManager" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:RazerBase="clr-namespace:RazerBase;assembly=RazerBase"
             xmlns:igDP="http://infragistics.com/DataPresenter" 
             xmlns:igEditors="http://infragistics.com/Editors"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1024"
             Height="Auto" Width="Auto" >
        

        <RazerBase:ScreenBase.Resources>
        
        <igDP:FieldLayout x:Key="RptMgrAdjDetailbyMo" x:Shared="False">
            
            <igDP:Field Name="company_code" Label="Company" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="acct_period" Label="Acctg Period" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrAdjDetailbyProduct" x:Shared="False">           
            
            <igDP:Field Name="from_date" Label="From Period" Width="110">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="to_date" Label="To Period" Width="110">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_code" Label="Product Code" Width="110">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductActive, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrAdsInvoiceRpt" x:Shared="False">
            <igDP:Field Name="document_date" Label="Document Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrAdsPrebillRpt" x:Shared="False">
            <igDP:Field Name="broadcast_month" Label="Broadcast Month" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrAdsRevShareRpt" x:Shared="False">
            <igDP:Field Name="service_period_start" Label="Service Period Start" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="service_period_end" Label="Service Period End" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrAgingwithHist" x:Shared="False">
            <igDP:Field Name="history_1" Label="History 1" Width="140">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCreditHist1, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="history_2" Label="History 2" Width="140">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCreditHist2, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="history_3" Label="History 3" Width="140">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCreditHist3, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrAgingbyProductRpt" x:Shared="False">

            <igDP:Field Name="company_code_all" Label="Company" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrAncillarySummarybyAcctPeriodRpt" x:Shared="False">
            <igDP:Field Name="acct_period" Label="Accounting Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>           
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrARCashbyAcctPeriodRpt" x:Shared="False">
            <igDP:Field Name="begin_period" Label="From Acct Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_period" Label="To Acct Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrRejectedAdjustments" x:Shared="False">
            <igDP:Field Name="start_date" Label="Start Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_date" Label="End Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrBadDebtRpt" x:Shared="False">
            <igDP:Field Name="service_period_start" Label="From Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="service_period_end" Label="To Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrBillingBatchRpt" x:Shared="False">
            <igDP:Field Name="batch_id" Label="Batch ID" Width="180"></igDP:Field>        
            
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrBillPeriodVarRpt" x:Shared="False">

            <igDP:Field Name="product_code" Label="Product" Width="180" Visibility="Collapsed"></igDP:Field>
            <igDP:Field Name="product_description" Label="Product" Width="300">
                <igDP:Field.Settings>
                    <igDP:FieldSettings AllowEdit="False" />
                </igDP:Field.Settings>
            </igDP:Field>

          
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrBillingVerRpt" x:Shared="False">

            <igDP:Field Name="company_code_all" Label="Company" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrBillingVerUnitRpt" x:Shared="False">

            <igDP:Field Name="product_type" Label="Product Type" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductTypeAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
         
        <igDP:Field Name="unit_md_category" Label="Unit Metadata" Width="180">
            <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
            <igDP:Field.Settings>
                <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                    <igDP:FieldSettings.EditorStyle>
                        <Style TargetType="{x:Type igEditors:XamComboEditor}">
                            <Setter Property="ItemsProvider" Value="{Binding Path=cmbUnitMetaData, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                        </Style>
                    </igDP:FieldSettings.EditorStyle>
                </igDP:FieldSettings>
            </igDP:Field.Settings>
        </igDP:Field>
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrCashPostedRpt" x:Shared="False">
           
            <igDP:Field Name="company_code_all" Label="Company" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="acct_period" Label="Acctg Period" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrContractExpiredRpt" x:Shared="False">
            <igDP:Field Name="company_code" Label="Company" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrCustHistoryTabRpt" x:Shared="False">
            <igDP:Field Name="receivable_account" Label="Customer ID" Width="180">
                
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrCustAgingTabRpt" x:Shared="False">
           
            <igDP:Field Name="product_code" Label="Product" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProduct, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="receivable_account" Label="Customer ID" Width="180">

            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrCustHistRpt" x:Shared="False">
            <igDP:Field Name="receivable_account" Label="Customer ID" Width="90">
            </igDP:Field>
            <igDP:Field Name="acct_period_start" Label="Start Date" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="acct_period_end" Label="End Date" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrCustHistSubsRpt" x:Shared="False">
            <igDP:Field Name="receivable_account" Label="Customer ID" Width="90">
            </igDP:Field>
            <igDP:Field Name="invoice_date_start" Label="Start Date" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="invoice_date_end" Label="End Date" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrCustReconRpt" x:Shared="False">
            <igDP:Field Name="customer_id" Label="Customer ID" Width="90">
            </igDP:Field>
            <igDP:Field Name="start_date" Label="Start Date" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_date" Label="End Date" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_code" Label="Product" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductNoAds, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrCustomInvRegRpt" x:Shared="False">

            <igDP:Field Name="product_type" Label="Product Type" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductTypeAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="acct_period" Label="Acctg Period" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

<igDP:FieldLayout x:Key=" RptMgrContractsmissingCustomersRpt" x:Shared="False">
           
        </igDP:FieldLayout>
        <igDP:FieldLayout x:Key=" RptMgrContractLocationsmissingCustomersRpt" x:Shared="False">

        </igDP:FieldLayout>

        

        <igDP:FieldLayout x:Key="RptMgrDailyCashRpt" x:Shared="False">
            <igDP:Field Name="date_posted" Label="Date Posted" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrDefReclassRpt" x:Shared="False">
            <igDP:Field Name="recog_period" Label="Recog Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrDefPoolRpt" x:Shared="False">

            <igDP:Field Name="company_code_all" Label="Company" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="given_date" Label="Acctg Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrRecDefRevRpt" x:Shared="False">
            <igDP:Field Name="start_period" Label="Start Period" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_period" Label="End Period" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>



        <igDP:FieldLayout x:Key="RptMgrEntityMCAAddrRpt" x:Shared="False">

            <igDP:Field Name="product_code" Label="Product" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
           
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrContractLocationRpt" x:Shared="False">

            <igDP:Field Name="product_code" Label="Product" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>

        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrFPandARpt" x:Shared="False">
            <igDP:Field Name="start_period" Label="From Period" Width="110">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_period" Label="To Period" Width="110">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_type" Label="Product Type" Width="110">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductTypeAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrFPandAUnitRpt" x:Shared="False">
            <igDP:Field Name="product_type" Label="Product Type" Width="110">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductTypeAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="start_period" Label="From Period" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_period" Label="End Period" Width="90">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
          
            <igDP:Field Name="use_billable_unit_flag" Label="Use Billable Unit Flag" Width="130">             
                
            </igDP:Field>
            
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrGLTransactionRpt" x:Shared="False">
            <igDP:Field Name="product_type" Label="Product Type" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductTypeAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="startdate" Label="From Period" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="enddate" Label="To Period" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrGLDefTransactionRpt" x:Shared="False">
            <igDP:Field Name="product_type" Label="Product Type" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductTypeAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="startdate" Label="From Period" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="enddate" Label="To Period" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrHoldRpt" x:Shared="False">
            <igDP:Field Name="product_code" Label="Product" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>

            <igDP:Field Name="company_code" Label="Company" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
           
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrLastInvoicebyLocation" x:Shared="False">

            <igDP:Field Name="product_code" Label="Product" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProduct, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>

        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrLocRuleOverrideRpt" x:Shared="False">
            <igDP:Field Name="override_date" Label="Override Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrModelsBilledRpt" x:Shared="False">
            <igDP:Field Name="start_period" Label="Service From Period" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_period" Label="Service End Period" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_code" Label="Product" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProduct, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>

            <igDP:Field Name="model" Label="Model" Width="150">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbModelDescAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>

            <igDP:Field Name="contract_id" Label="Contract ID" Width="90">
            </igDP:Field>

            <igDP:Field Name="use_billable_unit_flag" Label="Use Billable Unit Flag" Width="130">

            </igDP:Field>
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrMultiRenewalRpt" x:Shared="False">

            <igDP:Field Name="company_code" Label="Company" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_code" Label="Product" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>
        

        <igDP:FieldLayout x:Key="RptMgrOpsGlobalRpt" x:Shared="False">

            <igDP:Field Name="company_code" Label="Company" Width="170">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_code" Label="Product" Width="170">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrBillingInvoiceRegisterRpt" x:Shared="False">

            <igDP:Field Name="company_code" Label="Company" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_code" Label="Product" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="from_date" Label="From Acct Date" Width="87">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="to_date" Label="To Acct Date" Width="87">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="from_due_date" Label="From Due Date" Width="85">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="to_due_date" Label="To Due Date" Width="85">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrBillingInvoiceRegisterTaxRpt" x:Shared="False">

            <igDP:Field Name="company_code" Label="Company" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_code" Label="Product" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="from_date" Label="From Date" Width="100">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="to_date" Label="To Date" Width="100">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrOutofContractRpt" x:Shared="False">

            <igDP:Field Name="product_type" Label="Product Type" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductTypeAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="expiration_date" Label="Expiration Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrItemsByCoRpt" x:Shared="False">

            <igDP:Field Name="start_date" Label="Start Date" Width="180">
            <igDP:Field.Settings>
                <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
            </igDP:Field.Settings>
            </igDP:Field>

            <igDP:Field Name="product_type" Label="Product Type" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductTypeAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrInvoiceRegisterRpt" x:Shared="False">

            <igDP:Field Name="from_acct_period" Label="From Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="to_acct_period" Label="To Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrLaunchbyCustRpt" x:Shared="False">
            <igDP:Field Name="receivable_account" Label="Customer ID" Width="180">

            </igDP:Field>
           
            <igDP:Field Name="date_billed" Label="Date Billed" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

       

        <igDP:FieldLayout x:Key="RptMgrMissingRatesRpt" x:Shared="False">
            <igDP:Field Name="bill_period" Label="Bill Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrProductStatRpt" x:Shared="False">
           
            <igDP:Field Name="mso_type" Label="Entity Type" Width="75">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbMSOType, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="mso_id" Label="Entity" Width="220">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbMSO, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrResearchRpt" x:Shared="False">

            <igDP:Field Name="product_code" Label="Product" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="gl_co" Label="Company" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="ServicePeriod" Label="Service Period" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrRevbyStateRpt" x:Shared="False">

            <igDP:Field Name="product_type" Label="Product Type" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductType, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="bill_year" Label="Bill Year" Width="180">
                
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrRevperUnitRpt" x:Shared="False">
            <igDP:Field Name="start_period" Label="From Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_period" Label="End Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="company" Label="Company" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbCompanyAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="contract_id" Label="Contract ID" Width="180">
                
            </igDP:Field>
            <igDP:Field Name="customer_id" Label="Customer ID" Width="180">

            </igDP:Field>
            <igDP:Field Name="use_billable_unit_flag" Label="Use Billable Unit Flag" Width="180">
                
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrRevByCountryRpt" x:Shared="False">
            <igDP:Field Name="start_period" Label="Start Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_period" Label="End Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrAccountsNoCashRpt" x:Shared="False">
            <igDP:Field Name="begin_date" Label="Begin Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_date" Label="End Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrCashPostedByItemRpt" x:Shared="False">
            <igDP:Field Name="begin_date" Label="Begin Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_date" Label="End Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrSalesBillingRates" x:Shared="False">
            <igDP:Field Name="service_period" Label="Service Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>          
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrSalesTaxbyInvDtRpt" x:Shared="False">
            <igDP:Field Name="start_period" Label="From Invoice Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_period" Label="To Invoice Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrShipmentDataRpt" x:Shared="False">
            <igDP:Field Name="start_begin_date" Label="From Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="start_end_date" Label="To Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrStateSmryRpt" x:Shared="False">
           
            <igDP:Field Name="product_code" Label="Product" Width="180">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProduct, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="bill_period" Label="Bill Period" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrSubLastUpdatedRpt" x:Shared="False">
            <igDP:Field Name="product_type" Label="Product Type" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductTypeAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>

            <igDP:Field Name="billperiod" Label="Bill Period" Width="130">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>

        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrSubsByProductGrpRpt" x:Shared="False">
            <igDP:Field Name="product_code" Label="Product Group" Width="100">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductGroup, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>

            <igDP:Field Name="service_period_start" Label="Service Period Start" Width="130">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>

            <igDP:Field Name="service_period_end" Label="Service Period End" Width="130">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>

        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrUnpostedBillingRpt" x:Shared="False">
            <igDP:Field Name="batch_id" Label="Batch ID" Width="200">
                
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrContractPrescheduledBillingRpt" x:Shared="False">
            
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrContractUnfulfilledMilestonesRpt" x:Shared="False">
            
        </igDP:FieldLayout>

        



        <igDP:FieldLayout x:Key="RptMgrUnpostedDetailbyMSORpt" x:Shared="False">
            <igDP:Field Name="mso_id" Label="MSO" Width="220">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbMSO, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrEbifSubDetailRpt" x:Shared="False">
            <igDP:Field Name="acct_period" Label="Accounting Period" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_item_id" Label="Product Item" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProdItemEbif, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="subscriber_id" Label="Subscriber Type" Width="160">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbSubEbif, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>              
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrEbifSubSummaryRpt" x:Shared="False">
            <igDP:Field Name="acct_period" Label="Accounting Period" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="product_item_id" Label="Product Item" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProdItemEbif, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="subscriber_id" Label="Subscriber Type" Width="160">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbSubEbif, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>


        <igDP:FieldLayout x:Key="RptMgrUnpostedSmrybyMSORpt" x:Shared="False">
            <igDP:Field Name="mso_id" Label="MSO" Width="220">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbMSO, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

        <igDP:FieldLayout x:Key="RptMgrBillingReportsRecievedNotBilled" x:Shared="False">

            <igDP:Field Name="product_code" Label="Product" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="begin_date" Label="Begin Date" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_date" Label="End Date" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>

        </igDP:FieldLayout>
        <igDP:FieldLayout x:Key="RptBillingContractReviewDates" x:Shared="False">

            <igDP:Field Name="product_code" Label="Product" Width="120">
                <!--Code to do a drop down box in a grid or can be used as a text lookup for a value field for a non editable grid-->
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamComboEditor}" >
                        <igDP:FieldSettings.EditorStyle>
                            <Style TargetType="{x:Type igEditors:XamComboEditor}">
                                <Setter Property="ItemsProvider" Value="{Binding Path=cmbProductAll, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=2}}"/>
                            </Style>
                        </igDP:FieldSettings.EditorStyle>
                    </igDP:FieldSettings>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="begin_date" Label="Begin Date" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="end_date" Label="End Date" Width="120">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>

        </igDP:FieldLayout>
        <igDP:FieldLayout x:Key="RptContractRateDetailByProduct" x:Shared="False">

            
            <igDP:Field Name="product_code" Label="Product" Width="180" Visibility="Collapsed"></igDP:Field>
            <igDP:Field Name="product_description" Label="Product" Width="300">
                     <igDP:Field.Settings>
                        <igDP:FieldSettings AllowEdit="False" />
                    </igDP:Field.Settings>
            </igDP:Field>          


        </igDP:FieldLayout>
        <igDP:FieldLayout x:Key="RptMgrWHTAdjustmentRpt" x:Shared="False">
            <igDP:Field Name="from_date" Label="Begin Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
            <igDP:Field Name="to_date" Label="End Date" Width="180">
                <igDP:Field.Settings>
                    <igDP:FieldSettings EditorType="{x:Type igEditors:XamDateTimeEditor}"/>
                </igDP:Field.Settings>
            </igDP:Field>
        </igDP:FieldLayout>

    </RazerBase:ScreenBase.Resources>

        <Border Grid.Row="5" Grid.ColumnSpan="3" BorderBrush="Gray" BorderThickness="1,0,0,0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="450" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="500" />
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="15" />
                <RowDefinition Height="25" />
                <RowDefinition Height="375" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="*" />
                <RowDefinition Height="5" />
            </Grid.RowDefinitions>
            <!-- Place Controls Here -->
            <Label  Grid.Column="1" Grid.Row="1" FontSize="12" FontWeight="Bold">Reports Available to Submit</Label>
            <RazerBase:ucBaseGrid Name="gReportsAvailtoRun" Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="1"  />
            <Label  Grid.Column="3" Grid.Row="1" FontSize="12" FontWeight="Bold">Report Parameters</Label>
            <RazerBase:ucBaseGrid Name="gReportParms" Grid.Column="3" Grid.Row="2" Margin="0,0,0,0" Loaded="gReportParms_Loaded" />
            <Label  Grid.Column="1" Grid.Row="4" Grid.ColumnSpan="3" HorizontalAlignment="Center" FontSize="12" FontWeight="Bold">Completed Reports Available to View</Label>
            <RazerBase:ucBaseGrid Name="gRptstoView" Grid.Column="1" Grid.Row="5" Grid.ColumnSpan="3" LostFocus="gRptstoView_LostFocus"/>

                <!--<Border Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" BorderBrush="Gray"  BorderThickness="2" >-->
           
          
            <!--</Border>-->
          
        </Grid>
    </Border>

</RazerBase:ScreenBase>