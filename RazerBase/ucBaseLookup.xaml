<UserControl x:Class="RazerBase.ucBaseLookup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:igDP="http://infragistics.com/DataPresenter" 
             xmlns:igWindows="http://infragistics.com/Windows" 
             xmlns:local="clr-namespace:RazerBase"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800" Background="{StaticResource ApplicationBackground}">
    <Grid>
        <Grid.RowDefinitions>            
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="230" />
        </Grid.ColumnDefinitions>
        <!--<Grid Grid.ColumnSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition  />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

        </Grid>-->
        <local:ucBaseGrid x:Name="uGrid" x:FieldModifier="public" Grid.RowSpan="3" Grid.ColumnSpan="3" Grid.Row="0" ></local:ucBaseGrid>
        <Grid Grid.Column="2" Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="10*" />
            </Grid.ColumnDefinitions>
            
            <Button Name="bOK" Click="bOK_Click" Width="100" Height="23" Margin="10,60,0,16">OK</Button>
            <Button Name="bCancel" Width="100" Height="23" Grid.Column="1" Click="bCancel_Click" Margin="0,60,0,16" HorizontalAlignment="Right">Cancel</Button>
        </Grid>
    </Grid>
</UserControl>
